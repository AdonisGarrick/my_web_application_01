@model IEnumerable<ASM_02.Models.TrainerTopic>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Topic.tName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trainer.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Topic.tName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trainer.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ttID }) |
                @Html.ActionLink("Details", "Details", new { id = item.ttID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ttID })
            </td>
        </tr>
    }

</table>
