@model IEnumerable<ASM_02.Models.TraineeCourse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.cName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trainee.Location)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course.cName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trainee.Location)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.tcID }) |
                @Html.ActionLink("Details", "Details", new { id = item.tcID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.tcID })
            </td>
        </tr>
    }

</table>
